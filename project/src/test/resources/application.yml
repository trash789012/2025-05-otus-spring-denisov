spring:
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-test-master.yaml
    contexts: test
    enabled: true
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false